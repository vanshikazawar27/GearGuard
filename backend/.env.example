# Server Configuration
PORT=5000
NODE_ENV=development

# Database Selection - Set to 'true' to use SQLite (no password needed)
# Set to 'false' to use MySQL
USE_SQLITE=true

# MySQL Configuration (only used if USE_SQLITE=false)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=gearguard
DB_USER=root
DB_PASSWORD=your_password_here

# JWT Configuration
JWT_SECRET=gearguard_super_secret_jwt_key_2024_hackathon
JWT_EXPIRE=7d

# CORS
CORS_ORIGIN=http://localhost:5173
